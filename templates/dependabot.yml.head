# Generated by `acb init` from .acb.json's process.dependabotEcosystems.
# No ecosystems declared means no updates configured — the right default for a repository that
# has not decided, rather than a silent opt-in to every ecosystem.
version: 2
updates:
